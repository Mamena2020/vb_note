create macro in : Personal Macro Workbook

count data from top to down=> lastDataRow = dataSheet.Cells(startDataRow, "B").End(xlDown).Row
count data from bottom to up (last row cell) => lastDataRow = dataSheet.Cells(dataSheet.Rows.Count, "B").End(xlUp).Row

Enabled Scripting.Dictionary => Collection
1. Open Editor VBA => Alt + F11 di Excel.
2. Editor VBA, choose "Tools" > "References..."
3. On "References," search "Microsoft Scripting Runtime." and check box "Microsoft Scripting Runtime" and click "OK."



